#include "AVLTree.h"

int main()
{
    Tree tree = InitTree();

    Insert(tree, 12);
    Insert(tree, 4);
    Insert(tree, 1);
    Insert(tree, 3);
    Insert(tree, 7);
    Insert(tree, 8);
    Insert(tree, 10);
    Insert(tree, 9);
    Insert(tree, 2);
    Insert(tree, 11);
    Insert(tree, 6);
    Insert(tree, 5);
}
